// Placeholder hasta correr `supabase gen types typescript --project-id <id> > lib/types.ts`
// una vez creado el proyecto de Supabase (ver README de setup). Los helpers de
// lib/supabase/*.ts están tipados contra `Database`, así que basta con
// reemplazar este archivo por el generado sin tocar el resto del código.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
